const getUrlSearchParams = require('./get-url-search-params');
const getRandNumber = require('./get-rand-number');
const { Logger, ProductLogger } = require('./logger');

module.exports = {
	getUrlSearchParams,
	getRandNumber,
	Logger,
	ProductLogger,
};
